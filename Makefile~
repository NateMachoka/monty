CC = gcc
CFLAGS = -Wall -Werror -Wextra -pedantic -std=c89
OBJS = main.o stack.o opcode.o tokenizer.o error_handling.o memory_management.o

monty: $(OBJS)
    $(CC) $(CFLAGS) -o monty $(OBJS)

%.o: %.c monty.h
    $(CC) $(CFLAGS) -c $<

clean:
    rm -f monty $(OBJS)
